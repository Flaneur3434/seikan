name = "tests"
description = "VeriGuard test suite"
version = "0.1.0-dev"

authors = ["Your Name"]
maintainers = ["Your Name <example@example.com>"]
licenses = "MIT OR Apache-2.0 WITH LLVM-exception"
website = ""
tags = ["testing"]

# Local dependencies on project crates
[[depends-on]]
crypto = "*"

[[depends-on]]
wireguard = "*"

[[depends-on]]
net = "*"

# Pin paths to local crates (relative to tests/ada/)
[[pins]]
crypto = { path = "../../crypto" }

[[pins]]
wireguard = { path = "../../wireguard" }

[[pins]]
net = { path = "../../net" }
